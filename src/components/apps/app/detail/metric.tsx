import {Typography, List, Badge} from 'antd';
import Time from 'react-time-format';
import {} from '@/models/apps/apps';

export interface MetricsProps {
  data: any[];
}

const Metrics = ({data}: MetricsProps) => {
  return null;
};

export default Metrics;
